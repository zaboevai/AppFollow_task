import unittest
from unittest.mock import Mock
from parsers.core import Parser


# TODO cover code by tests
class HackerNewsTest(unittest.TestCase):

    # def setUp(self):
    #     db = Mock()
    #     lock = Mock()
    #     self.parser = Mock()
    #     SOURCE_URL = 'https://news.ycombinator.com'
    #     MAX_NEWS_COUNT = 30
    #     self.parser = Parser(url=SOURCE_URL,
    #                          news_count=MAX_NEWS_COUNT,
    #                          sleep_time=10,
    #                          test_mode=False,
    #                          lock=lock)

    def test_start(self):
        pass
